# Origin of the Omnion API the admin panel forwards `/api/*` to (next.config.ts).
# The default matches the development stack: infra/compose + `cargo run -p omnion-api`.
OMNION_API_URL=http://127.0.0.1:8080
