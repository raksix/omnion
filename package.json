{
  "name": "omnion",
  "version": "0.1.0",
  "private": true,
  "description": "Omnion — an open-source enterprise application platform with a powerful CMS at its core.",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20.9"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.11.4"
  }
}
